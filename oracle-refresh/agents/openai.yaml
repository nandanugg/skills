interface:
  display_name: "Oracle Refresh"
  short_description: "Refresh durian-oracle from services and validation lanes"
  default_prompt: "Use $oracle-refresh to pull the latest durian-oracle and service repos, refresh service docs first, update downstream docs from that baseline, and then run service-led validation."
